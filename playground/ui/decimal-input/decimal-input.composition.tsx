import React from 'react';
import { DecimalInput } from './decimal-input';

export const BasicDecimalInput = (): React.ReactElement => (
  <DecimalInput id={'test-decimal-input-1'} value={1234.5} />
);
